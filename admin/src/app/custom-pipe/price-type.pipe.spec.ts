import { PriceTypePipe } from './price-type.pipe';

describe('PriceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
