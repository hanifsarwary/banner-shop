import { OptionTypesPipe } from './option-types.pipe';

describe('OptionTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
